'use strict';

require('should');

var eml = require('../lib/');


describe('Test EML results', function() {
  it('returns the correct informations', function(done) {
    var document = {
      datas: {}
    };
    done();
  });
});
